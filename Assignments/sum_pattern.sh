#!/bin/bash


read number

for i in 1  
do
	echo "$((number + 1)) $((number + 2))"
	echo "$((number +3)) $((number +4)) $((number +5))"
	echo "$((number +6)) $((number +7)) $((number +8)) $((number +9))"
	
done

